
service: midsomerplots-content

provider:
  name: aws
  runtime: nodejs10.x
  stage: prod
  region: us-east-1

functions:
  getPlot:
    handler: index.handler
    events:
      - http:
          path: plot
          cors: true
          method: get